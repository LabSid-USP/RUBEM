name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v3
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        issue_message: 'Welcome to RUBEM community! Thanks so much for creating your first issue :)'
        pr_message: 'Thanks so much for creating your first PR, the RUBEM community thanks you :)'
