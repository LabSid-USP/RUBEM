[pytest]
markers =
    slow: mark test as slow.
    unit: mark a test as a unit test.
    integration: mark a test as a integration test.
    exact: mark a test that reproduces the goldens byte-for-byte on the golden environment.
